<Page navigatingTo="onNavigatingTo" class="page" xmlns="http://schemas.nativescript.org/tns.xsd">
    <ActionBar class="action-bar">
        <Label class="action-bar-title" text="Login"></Label>
    </ActionBar>

    <ScrollView>
        <StackLayout class="p-t-15">

            <Label 
                class="text-muted p-10" 
                text="{{ mnemonic }}" 
                textWrap="true"
                fontSize="18" />

            <Button class="btn btn-primary" text="Create Account" tap="onCreateAccount"/>
            <Button class="btn btn-primary" text="Retrieve Account" tap="onRetrieveAccount"/>

        </StackLayout>
    </ScrollView>
</Page>